# ===========================================
# INFRASTRUCTURE SETTINGS (Environment Variables Only)
# These settings cannot be changed via UI
# ===========================================

# System (Docker only)
PUID=1000
PGID=1000
TZ=Europe/Istanbul
NODE_ENV=production

# Server
PORT=3001
HOST=0.0.0.0

# Security (REQUIRED - Change in production!)
SESSION_SECRET=change-this-to-a-random-secret
SESSION_COOKIE_NAME=eros-session
ADMIN_PASSWORD=admin

# Database
DATABASE_PATH=/data/app.db

# Media Path - Mounted volume root (Docker only)
# This path will be shared across all services (server, qBittorrent)
MEDIA_PATH=./media

# FlareSolverr (Optional)
FLARESOLVERR_PORT=8191
LOG_LEVEL=info
LOG_HTML=false
CAPTCHA_SOLVER=none

# ===========================================
# APPLICATION SETTINGS (Configure via Web UI)
# Do NOT set these as environment variables
# They are managed through the settings page
# ===========================================

# The following are configured via UI:
# - StashDB (API URL, API Key)
# - Prowlarr (API URL, API Key)
# - qBittorrent (URL, Username, Password)
# - AI Matching (Enable, Model, Threshold)
# - File Paths (Download, Scenes, Incomplete)
# - Job Schedules (All job cron expressions and enable flags)
# - File Management Options
